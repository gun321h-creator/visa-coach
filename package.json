{
  "name": "visa-coach",
  "version": "0.1.0",
  "description": "Voice-based B1/B2 visa interview practice agent built on AssemblyAI's Voice Agent API",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "node --env-file=.env server.js",
    "smoke": "node --env-file=.env scripts/smoke.mjs",
    "guards": "node scripts/test_contradiction_guards.mjs",
    "test": "node scripts/test_contradiction_guards.mjs && node --env-file=.env scripts/smoke.mjs"
  },
  "engines": {
    "node": "22.x"
  }
}
